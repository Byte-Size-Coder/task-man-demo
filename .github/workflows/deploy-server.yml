name: Deploy Server Project With Render

on:
    push:
        branches: [main]
        paths:
            - 'server/**'
            
jobs:
    deploy-server:
        runs-on: ubuntu-latest

        steps:
        - name: Render Deploy Server
          run: curl ${{ secrets.RENDER_SERVER_HOOK }}